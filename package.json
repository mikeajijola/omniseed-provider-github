{
  "name": "@omniseed/provider-github-reference",
  "version": "0.1.0-alpha.2",
  "private": true,
  "description": "Narrow GitHub implementation of OmniSeed Provider Protocol v1.",
  "scripts": {
    "test": "python3 -m unittest discover -s test -p 'test_*.py'",
    "acceptance": "node test/live-acceptance.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
